# Hydra batch 01

Four full sheets generated with the built-in tool. Requested128 cells; observed124: south/north/east32 each, west28 because it returned seven rows. These are visible source figures, not124 distinct approved frames. Four action families: walk, bite, hurt, defeat. Runtime admission0.

South/north use4x8 onportrait canvases (square cells). East/west use4x8 onSQUARE canvases to provide twice-as-wide cells for the long body. Exactly three heads remain the identity requirement. Sources preserve the established green/gold palette and dark pixel outlines; no raster editing or masking was applied. Exact prompts and SHA256 hashes are in the manifest and prompt files.

Native75frame preview plays twelve sequences from the regular south/north/east grids with the actual PixelActor key shader. West is explicitly excluded from automatic slicing until its seven-row action structure is remapped. Review shows useful biting/falling arcs, but gait alternation, repeat-looking hurt poses, head overlap and uneven scale across directions need admission work. The preview scales for inspection and is not a game replacement.

Tools: tools/review_hydra_batch.gd. Output: outputs/Hydra-Batch-01-Preview.mp4. Shared unchanged sources and manifest: shared-game-environment-library/assets/candidates_needing_review/olympus-hydra-batch-01-2026-09-08. Existing runtime Hydra clips remain authoritative.
