# Hoplite batch 01

Four built-in generation calls produced four unchanged 1536x1024 sheets: 128 requested cells, 127 visible figure drawings because the north thrust row contains seven wide figures. These are candidates, not 127 distinct approved sprites. Exact prompts and hashes are in the batch manifest. Shared copy: shared-game-environment-library/assets/candidates_needing_review/olympus-hoplite-batch-01-2026-09-08.

South offers shield brace, hurt, kneeling defeat and victory. North offers walk, thrust, hurt and forward defeat. East offers walk, run, shield bash and defeat. West offers walk, shield defense, hurt and defeat. Reactions/defeat provide useful-looking variety. East/west gait rows repeat leading-leg silhouettes and must not replace existing accepted walks.

Native 75-frame preview runs15 candidate rows through PixelActor and the real magenta key shader; irregular north thrust is skipped. The uniform-grid preview exposes additional spear/neighbor bleed, especially west rows. These rows require measured crops or full-sheet correction before runtime admission. The preview is a curation tool and deliberately does not imply production approval. Runtime-admitted drawings:0.

Review: tools/review_hoplite_batch.gd --batch-output=<absolute folder>. Outputs/Hoplite-Batch-01-Preview contains sequential frames; source raster pixels remain unchanged. South defeat is a priority for measured grounding and integration because current character removal only dissolves the last pose.
