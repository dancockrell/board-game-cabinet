# Retired 3D model-production contract

Superseded 2026-09-08 by the user's explicit **2D-only** direction. The old GLB/model contract and procedural fallback are not active instructions and must not be implemented or restored.

The original document is preserved, with SHA256 verification, in the shared `olympus-legacy-2d-only-2026-09-08` archive. The active production authority is [the 2D board-art contract](OLYMPUS_BOARD_ART.md), the per-character sprite contracts, and [the current handoff](HANDOFF.md).

Use authored sprite sheets, explicit atlas regions, stable pivots, sequential timing, proper alpha or chroma handling, and native gameplay review. Use only the built-in image generator authorized by the user. Old source portraits, model sheets, material trials, and model reviews are historical evidence, not instructions to resume 3D work.

## Global 2D production decision

The 2026-09-08 owner decision applies to every project and shared pack: 2D artwork only. Delete retired 3D assets rather than preserve them for a future return. Reuse the shared 2D art folder across Olympus, Cattle Trail, DR Companion and Pirate Island. Physical removal outside this checkout is pending: automatic approval review rejected deletion with only "blocked by policy". Do not describe an inventory or a catalog exclusion as completed deletion.
